import { HandleVoidValuePipe } from './handle-void-value.pipe';

describe('HandleVoidValuePipe', () => {
  it('create an instance', () => {
    const pipe = new HandleVoidValuePipe();
    expect(pipe).toBeTruthy();
  });
});
